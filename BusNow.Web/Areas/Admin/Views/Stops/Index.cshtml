@model IEnumerable<BusNow.Core.Entities.Stop>

@{
    ViewData["Title"] = "Спирки";
}

<div class="mb-4">
    <h1 class="page-title">Спирки</h1>
    <p class="section-subtitle">Управление на спирки и техните координати.</p>
</div>

<div class="mb-3">
    <a asp-action="Create" class="btn btn-success">Създай спирка</a>
</div>

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table align-middle">
                <thead>
                    <tr>
                        <th>Име</th>
                        <th>Код</th>
                        <th>Ширина</th>
                        <th>Дължина</th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td><strong>@item.Name</strong></td>
                            <td>@item.Code</td>
                            <td>@item.Latitude</td>
                            <td>@item.Longitude</td>
                            <td class="d-flex gap-2 flex-wrap">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-primary">Редакция</a>
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">Детайли</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">Изтрий</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>