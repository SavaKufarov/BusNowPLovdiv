@model BusNow.Core.Entities.Schedule

@{
    ViewData["Title"] = "Изтриване на разписание";
}

<div class="mb-4">
    <h1 class="page-title">Изтриване на разписание</h1>
    <p class="section-subtitle">Потвърждение за изтриване.</p>
</div>

<div class="card border-danger">
    <div class="card-body">
        <div class="alert alert-danger">
            Сигурен ли си, че искаш да изтриеш това разписание?
        </div>

        <p><strong>Линия:</strong> @Model.TransportLine?.LineNumber</p>
        <p><strong>Маршрут:</strong> @Model.Route?.Name</p>
        <p><strong>Час:</strong> @Model.DepartureTime.ToString(@"hh\:mm")</p>

        <form asp-action="Delete" class="mt-4">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-danger">Изтрий</button>
                <a asp-action="Index" class="btn btn-secondary">Назад</a>
            </div>
        </form>
    </div>
</div>