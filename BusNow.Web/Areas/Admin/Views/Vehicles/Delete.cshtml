@model BusNow.Core.Entities.Vehicle

@{
    ViewData["Title"] = "Изтриване на превозно средство";
}

<div class="mb-4">
    <h1 class="page-title">Изтриване на превозно средство</h1>
    <p class="section-subtitle">Потвърждение за изтриване.</p>
</div>

<div class="card border-danger">
    <div class="card-body">
        <div class="alert alert-danger">
            Сигурен ли си, че искаш да изтриеш това превозно средство?
        </div>

        <p><strong>Линия:</strong> @Model.TransportLine?.LineNumber</p>
        <p><strong>Маршрут:</strong> @Model.Route?.Name</p>
        <p><strong>Регистрационен номер:</strong> @Model.RegistrationNumber</p>
        <p><strong>Тип:</strong> @Model.VehicleType</p>

        <form asp-action="Delete" class="mt-4">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-danger">Изтрий</button>
                <a asp-action="Index" class="btn btn-secondary">Назад</a>
            </div>
        </form>
    </div>
</div>